{{- include "ffc-helm-library.container-config-map" (list . "ffc-tcg-back-office.config-map") -}}
{{- define "ffc-tcg-back-office.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
